# The media-loop panel.
#
# The addon's own metadata, installed beside ailoop.so and read INSTEAD
# of it: the host has to know what a widget IS before it decides to
# load anybody's code. The crate includes these very bytes when it
# is linked into the program rather than shipped as a file, so the
# two can never drift.
#
#   label      the name the layout editor shows (default: the file
#              stem in capitals)
#   ref_h      height in vh at which the widget draws at 100% scale
#   min_h      height in vh below which its last content row would
#              leave the screen
#   category   board (the default), appgrid or search_and_ai — the
#              kind of board this widget may be placed on
#   slot       left, center or right — which column of a GENERATED
#              arrangement this widget asks for (default: whichever
#              side has room). There is no table of widgets in the
#              program: where a widget stands is what it says here
#   order      where in that column, lowest first
#   weight     how much of a shared column it wants (default: as
#              much of it as the widget is tall)
#   anchor     top, bottom or bar — a pinned edge of the arrangement
#              instead of flowing with the column
#   essential  true: switching this widget off would leave the user
#              no way back, so the editor never offers to remove it
#
# Everything is optional: an addon that names nothing is still an
# addon, and what it does not name it does not lose.
#
# How the SEARCH AND AI board is shared: search declares no slot and
# takes the centre, the room a query's answers need. The right column
# is the AI's — CHAT leads it (order 10, a double share: a
# conversation is the tall one), and the tools stack under it in
# tens: loop at 20, sort at 30. Each widget states its own line here;
# nothing owns the arrangement, and the gaps in the numbering are
# where the next tool goes without renumbering anybody.
#
# The two heights are a PATH FIELD, a button row and a few lines of
# the daemon's progress: at ref_h all three have room and a finished
# run's file path is readable under them; at min_h the field, the
# button and one status line still fit, and a tool that cannot say
# what it is doing is not a tool.

label = MEDIA LOOP
ref_h = 16.0
min_h = 8.0
category = search_and_ai
slot = right
order = 20
